# web-script-injectors
Framework-agnostic JS snippet injectors for the oriz fleet. Strict regex allowlist on every env value, SafeHtml branded type, silent skip on missing/invalid creds. Used by api-fleet-template.
